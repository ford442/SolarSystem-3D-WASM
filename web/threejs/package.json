{
  "name": "solar-system-threejs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "assets:prepare": "node scripts/prepare-assets.mjs",
    "assets:transcode": "node scripts/transcode-dds-to-ktx2.mjs",
    "dev": "npm run assets:prepare && vite",
    "build": "npm run assets:prepare && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@types/three": "^0.184.1",
    "three": "^0.184.0"
  }
}
